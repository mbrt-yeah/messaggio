export interface SerializableInterface {
    /**
     * Serializes the instance of the class which implements this interface to a string
     * 
     * @returns The serialized instance of the class
     */
    toString(): string;
}